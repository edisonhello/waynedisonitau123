#include <bits/stdc++.h>
using namespace std;

set<int> watching[200005];

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    int last = 0, pid = 0;
    int n, m; cin >> n >> m; while (m--) {
        int c; cin >> c; 
        if (c == 1) {

        }
    }
}
