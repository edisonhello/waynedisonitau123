#include <bits/stdc++.h>
using namespace std;

const int kN = 1000 + 5;
char s[kN];

int main() {
    int n; scanf("%d", &n);
    for (int i = 0; i < n; ++i) scanf("%s", s[i]);

    for (int i = 0; i < n; ++i) {
        int k = strlen(s[i]);
        for (int j = 1; j <= k; ++j) {

        }
    }
}
